[package]
name = "spack-spec-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]

[dev-dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
